# Generated practice artifacts — rebuild with: cd sql && python3 build_db.py
sql/prep.db
sql/seed.sql

# Local Postgres data dir (if you initdb one inside the repo)
sql/.pgdata/

# Python
__pycache__/
*.pyc

# Personal scratch SQL (write your attempts here without committing)
sql/*scratch*.sql
